package(default_visibility = ["//visibility:public"])

filegroup(
    name = "img",
    srcs = glob(
        ["**/*"],
        exclude = ["**/*.bazel"],
    ),
    data = glob(
        ["**/*"],
        exclude = ["**/*.bazel"],
    ),
)
